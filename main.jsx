import React from 'react';
import ReactDOM from 'react-dom/client';
import GPTChatSimulation from './CustomsGPTChatSimulation';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GPTChatSimulation />
  </React.StrictMode>
);